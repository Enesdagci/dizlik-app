name: dizlik_takip_sistemi
description: Diz Artroplastisi Sonrası Termal İzleme Sistemi
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # ÖNEMLİ: Bu satır flutter altında olmalı, aynı hizada
  flutter_localizations:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  flutter_blue_plus: ^1.32.11
  permission_handler: ^11.3.1
  fl_chart: ^0.69.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.4
  share_plus: ^10.0.3
  mobile_scanner: ^5.2.3
  uuid: ^4.5.1
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13

flutter:
  uses-material-design: true